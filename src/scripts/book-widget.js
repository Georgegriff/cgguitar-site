// const planWidget = document.querySelector("#tz-plan-widget-new");
// const config = { childList: true, subtree: true };

// let initialisedForm = false;
// const callback = () => {
//     const planForm = planWidget.querySelector(".tz-plan-wg-plan");
//     debugger;
//     const lessonTypeOption = [...planForm.options].find((option) => option.value !== "0");
//     if (lessonTypeOption) {
//         lessonTypeOption.selected = true;
//         initialisedForm = true;
//         observer.disconnect();
//     }
// };
  
//   // Create an observer instance linked to the callback function
// const observer = new MutationObserver(callback);
  
// // Start observing the target node for configured mutations
// observer.observe(planWidget, config);
 
// doesnt currently work
