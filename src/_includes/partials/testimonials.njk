{%- set testimonials = testimonials or collections.testimonial -%}
<div class="width-container background-50">
    <article style="padding-top:calc(2 * var(--site-padding));" id="students">
        {%- include "partials/components/starRating.njk" -%}
        <ul class="testimonials masonry">
        {%- asyncEach testimonial in testimonials | reverse %}
            {%- include "partials/components/testimonial.njk" %}
        {%- endeach %}
        </ul>
    </article>
</div>