<section>
{%- set quote = quote or component %}
{%- if quote %}
<div id="{{quote.data.name | slugify }}" class="quote-card width-container">
    <blockquote>{{quote.templateContent | safe}}</blockquote>
</div>
{%- endif %}
</section>